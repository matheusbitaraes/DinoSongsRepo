
describe("REST requests", () => {

    test('Return healty', () => {
    })
    
    test('Fails to register with bad e-mail', () => {
        expect(1+2).toBe(3)
    })
    
    test('Fails to register with password', () => {
        expect(1+2).toBe(3)
    })
    
    test('Fails to login unknown user', () => {
        expect(1+2).toBe(3)
    })
    
    test('Return all notes for user', () => {
        expect(1+2).toBe(3)
    })
    
    test('Return no notes, because no user', () => {
        expect(1+2).toBe(3)
    })
    
    test('Return a single note', () => {
        expect(1+2).toBe(3)
    }) 
})

test('Test Sum', () => {
    expect(1+2).toBe(3)
})